repcoach/
  app/                    # webcam UI
  pose/                   # mediapipe + smoothing + normalization
  features/               # angles, velocities, ROM
  counting/               # FSM logic per exercise
  form/                   # rules, no-rep logic
  data/repcount/          # loaders + splits
  eval/                   # metrics + matching + reports
  tests/                  # unit + integration
  scripts/                # run_webcam.py, eval_repcount.py
